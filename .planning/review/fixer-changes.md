# FIXER-CHANGES.md

> Auto-generated by Fixer subagent (2026-05-10)

---

## Summary of Changes

| # | Issue | Severity | File(s) Changed | Status |
|---|-------|----------|-----------------|--------|
| 1 | No auth on API routes | CRITICAL | `src/lib/auth-helpers.ts` (new) + 5 route files | ✅ |
| 2 | Dashboard hardcoded | CRITICAL | `src/app/page.tsx` | ✅ |
| 3 | Grab form non-functional | HIGH | `src/app/grab/page.tsx` | ✅ |
| 4 | Subscriptions count/Switch | HIGH | `src/app/subscriptions/page.tsx` | ✅ |
| 5 | next.config.ts orphan promise | HIGH | `next.config.ts` | ✅ |
| 6 | Grab mock data | HIGH | `src/app/grab/page.tsx` (covered by #3) | ✅ |
| 7 | `catch (e: any)` | MEDIUM | `src/app/debt/page.tsx` | ✅ |
| 8 | `paidThisMonth` status check | MEDIUM | `src/app/debt/page.tsx` | ✅ |
| 9 | D1 adapter documentation | CRITICAL | `src/lib/db.ts` | ✅ |
| 10 | Environment-aware error messages | MEDIUM | All 5 API route files (covered by #1) | ✅ |
| 11 | Grab API input validation | MEDIUM | `src/app/api/grab/route.ts` (covered by #1) | ✅ |
| 12 | Payments API body.id validation | MEDIUM | `src/app/api/payments/route.ts` (covered by #1) | ✅ |

---

## Detailed Changes

### 1. 🔴 Auth helper + applied to ALL API routes

**New file:** `src/lib/auth-helpers.ts`
- `getSession()` — wraps `auth.api.getSession()` with error handling
- `requireAuth()` — returns session or 401 Response
- `requireAuthTuple()` — returns `[Response, null]` or `[null, session]` tuple for cleaner usage

**Updated files:**
- `src/app/api/debt/route.ts` — GET + POST both check auth
- `src/app/api/debt/[id]/route.ts` — PATCH + DELETE both check auth
- `src/app/api/debt/[id]/pay/route.ts` — POST checks auth
- `src/app/api/grab/route.ts` — GET + POST both check auth + field-level validation (hours, gross, etc.)
- `src/app/api/payments/route.ts` — GET + POST + PATCH all check auth + `body.id` validation on PATCH

All routes now return `401 { error: "Authentication required" }` when no valid session exists.

**Bonus:** Error messages are now environment-aware — dev shows real error messages, production uses generic safe messages.

### 2. 🔴 Dashboard — real data from API

**File:** `src/app/page.tsx`

**Before:** Hardcoded `debts[]` and `upcoming[]` arrays. Income and surplus were magic numbers.
**After:**
- `useEffect` on mount fetches from `/api/debt`, `/api/payments`, `/api/grab`
- Removed all hardcoded arrays
- Added loading state (spinner)
- Added error state (red card)
- Summary cards derived from real data:
  - Income = salary (3000) + grab this month (from `/api/grab`)
  - Debt = sum of all debt balances
  - Surplus = income - commitments - estimated costs
  - Surplus color is conditional: green for positive, red for negative
- Upcoming payments fetched from API, filtered to pending only
- Added proper TypeScript interfaces for API response types

### 3. 🔴 Grab form — fully functional

**File:** `src/app/grab/page.tsx`

**Before:** Form had no `onSubmit`, no state management, data was hardcoded (weekData, stats).
**After:**
- Added form state via `useState` for all fields (date, platform, hours, gross, fuel, net)
- Added `onSubmit` handler that POSTs to `/api/grab` with proper JSON body
- Added loading/saving state with spinner
- Added success/error feedback messages
- Removed all hardcoded `weekData` — week chart now computed from API data
- Stats (This Week, This Month, Avg/Day) derived from real entries
- Added Cancel button to form
- Added loading and error states for page init

### 4. 🟠 Subscriptions page — fixes

**File:** `src/app/subscriptions/page.tsx`

- **Count fix:** Changed `{subscriptions.length} active services` → `{activeCount}` where `activeCount = subscriptions.filter(s => s.active).length`
- **Switch fix:** Changed `defaultChecked={sub.active}` → `checked={sub.active}` with `onCheckedChange` handler that toggles state in-memory
- **Layout fix:** Removed `min-h-screen bg-black pt-16`, replaced with `pt-20` consistent with other pages

### 5. 🟠 next.config.ts

**Before:**
```ts
export default nextConfig;
import('@opennextjs/cloudflare').then(m => m.initOpenNextCloudflareForDev());
```
**After:**
```ts
export default nextConfig;
if (process.env.OPENNEXT_CLOUDFLARE === "1") {
  import("@opennextjs/cloudflare")
    .then((m) => m.initOpenNextCloudflareForDev())
    .catch((err) => console.error("Cloudflare init failed:", err));
}
```
Runs only when `OPENNEXT_CLOUDFLARE=1` env var is set. Promise is caught for error handling.

### 6. 🟡 `catch (e: any)` → `catch (e: unknown)`

**File:** `src/app/debt/page.tsx`

**Before:** `} catch (e: any) { setPaySuccess(`❌ ${e.message}`); }`
**After:** `} catch (e: unknown) { const message = e instanceof Error ? e.message : "Payment failed"; ... }`

### 7. 🟡 `paidThisMonth` — status check

**File:** `src/app/debt/page.tsx`

**Before:** Filter used `new Date(p.paidDate || p.dueDate)` — counted pending payments with a dueDate in current month as "paid".
**After:** Added `if (p.status !== "paid") return false;` before the date check.

### 8. 🟡 D1 adapter — documentation

**File:** `src/lib/db.ts`

Added docstring explaining how to swap to D1 adapter for Cloudflare deployment, with commented-out code example. Local `PrismaClient` remains unchanged for dev.

---

## TypeScript Check

`npx tsc --noEmit` → **passes with zero errors** ✅
